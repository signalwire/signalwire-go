# SignalWire RELAY Client

The RELAY client documentation lives in one place to avoid drift:

**➡ [relay/README.md](../../relay/README.md)** — the canonical RELAY guide, quick
start, call-methods reference, events, messaging, and examples.

This file is a pointer only. The Go source for the RELAY client is in this package
(`pkg/relay/`); the user-facing documentation is maintained at the repo-level
[`relay/`](../../relay/) docs hub.
