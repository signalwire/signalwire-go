# SignalWire REST Client

The REST client documentation lives in one place to avoid drift:

**➡ [rest/README.md](../../rest/README.md)** — the canonical REST guide, API
reference, namespace list, and examples.

This file is a pointer only. The Go source for the REST client is in this package
(`pkg/rest/`); the user-facing documentation is maintained at the repo-level
[`rest/`](../../rest/) docs hub.
